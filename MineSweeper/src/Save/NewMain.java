package Save;

import GUI.CreateUser;
import GUI.GameAccess;
import GUI.HistoryList;
import java.awt.event.WindowAdapter;
import java.awt.event.WindowEvent;
import javax.swing.JFrame;
import javax.swing.JOptionPane;

        
        
        
        
        
        
        
  /*      import javax.swing.*;

public class NewMain {

    public NewMain() {
        int result;
        String[] modes = {"A", "B", "C"};
        result = JOptionPane.showOptionDialog(
                null,
                "Modes",
                "Select mode",
                JOptionPane.OK_CANCEL_OPTION,
                JOptionPane.QUESTION_MESSAGE,
                null,
                modes,
                "B"
        );
        if (result == JOptionPane.CLOSED_OPTION) {
            System.out.println("User canceled mode selection");
        } else {
            System.out.println("Result: " + modes[result]);
        }
        result = JOptionPane.showConfirmDialog(null,
                "Are you now, or have you ever?",
                "Declaration",
                JOptionPane.YES_NO_OPTION);
        if (result == JOptionPane.YES_OPTION) {
            System.out.println("User says YES!");
        } else if (result == JOptionPane.NO_OPTION) {
            System.out.println("User says NO!");
        } else {
            System.out.println("User canceled");
        }
    }

    public static void main(String[] args) {
        Runnable r = () -> new NewMain();
        SwingUtilities.invokeLater(r);
    }
}*/
        
public class NewMain {

   /* public static void main(String[] args) {
                try {
            for (javax.swing.UIManager.LookAndFeelInfo info : javax.swing.UIManager.getInstalledLookAndFeels()) {
                if ("Nimbus".equals(info.getName())) {
                    javax.swing.UIManager.setLookAndFeel(info.getClassName());
                    break;
                }
            }
        } catch (ClassNotFoundException ex) {
            java.util.logging.Logger.getLogger(GameAccess.class.getName()).log(java.util.logging.Level.SEVERE, null, ex);
        } catch (InstantiationException ex) {
            java.util.logging.Logger.getLogger(GameAccess.class.getName()).log(java.util.logging.Level.SEVERE, null, ex);
        } catch (IllegalAccessException ex) {
            java.util.logging.Logger.getLogger(GameAccess.class.getName()).log(java.util.logging.Level.SEVERE, null, ex);
        } catch (javax.swing.UnsupportedLookAndFeelException ex) {
            java.util.logging.Logger.getLogger(GameAccess.class.getName()).log(java.util.logging.Level.SEVERE, null, ex);
        }

        java.awt.EventQueue.invokeLater(new Runnable() {
            public void run() {
               // HistoryList hisrotyList = new HistoryList(5);
                
            }
        });


    }*/
}
 /*       int result = JOptionPane.showConfirmDialog(null, "Press OK to get a frame",
                "Title", JOptionPane.OK_CANCEL_OPTION);

        if (result == JOptionPane.OK_OPTION) {
            System.err.println("welcome");
        } else if (result == JOptionPane.CANCEL_OPTION) {
            System.err.println("beeee");
        }

        int resulta = JOptionPane.showConfirmDialog(null, "Press OK to get a frame");
        if (resulta == JOptionPane.YES_OPTION) {
              System.err.println("welcome");
        } else if (resulta == JOptionPane.NO_OPTION) {
             System.err.println("beeee");
        }
        int resultw = JOptionPane.showConfirmDialog(null, "Press OK to get a frame");
                            if (resultw == JOptionPane.YES_OPTION) {
                                   System.err.println("welcome");
                            } else if (resultw == JOptionPane.NO_OPTION) {
                                 System.err.println("beeee");
                            }
    }

}*/
